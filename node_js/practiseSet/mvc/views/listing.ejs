<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listing</title>


    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: #f4f4f4;
        }
        .navbar {
            background: #333;
            padding: 15px;
            text-align: center;
        }
        .navbar a {
            color: white;
            margin: 0 15px;
            text-decoration: none;
            font-size: 18px;
            transition: color 0.3s;
        }
        .navbar a:hover {
            color: #f39c12;
        }
        .container {
            max-width: 500px;
            margin: 50px auto;
            background: whitesmoke;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 5px 0 20px rgba(0, 0, 0, 0.1);
        }

    </style>
</head>
<body>
    <div class="navbar">
        <a href="/">Home</a>
        <a href="/host/listing">Listings</a>
        <a href="/user/contact">Contact</a>
    </div>

    <% if (details && details.length > 0) { %>  <!-- ✅ Check if details exist -->
        <% details.forEach(detail => { %>  
            <br>
            <div class="container">
                <h3>Name: </h3><%= detail.name %>
                <h3>Address: </h3><%= detail.address %>
                <h3>House Name</h3><%= detail.housename %>
                <h3>Price: </h3><%= detail.price %>
                <h3>Rating: </h3><%= detail.rating %>
                <h3>Image URL: </h3><%= detail.image %>
            </div>
        <% }) %>  
    <% } else { %>
        <div class="container">No listings available.</div>
    <% } %>
    
    
</body>
</html>